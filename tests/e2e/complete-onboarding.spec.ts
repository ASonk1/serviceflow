import AxeBuilder from "@axe-core/playwright";
import {expect,test} from "@playwright/test";
const email="phase3b-owner@serviceflow.test",password="Fictional-phase3b-password-12",slug="fictional-linden-studio";
const png=Buffer.from("iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+A8AAQUBAScY42YAAAAASUVORK5CYII=","base64");
async function confirmationLink(request:import("@playwright/test").APIRequestContext){for(let i=0;i<30;i++){const response=await request.get("http://127.0.0.1:54324/api/v1/messages");const body=await response.json() as {messages:{ID:string;To:{Address:string}[]}[]};const message=body.messages.find(m=>m.To.some(to=>to.Address===email));if(message){const detail=await request.get(`http://127.0.0.1:54324/api/v1/message/${message.ID}`);const mail=await detail.json() as {HTML?:string;Text?:string};const match=(mail.HTML??mail.Text??"").match(/https?:\/\/[^\s"'<>]+/);if(match)return match[0].replace(/&amp;/g,"&")}await new Promise(resolve=>setTimeout(resolve,200))}throw new Error("Fictional Phase 3B confirmation did not arrive.")}
async function invitationLink(request:import("@playwright/test").APIRequestContext,target:string){for(let i=0;i<30;i++){const response=await request.get("http://127.0.0.1:54324/api/v1/messages");const body=await response.json() as {messages:{ID:string;To:{Address:string}[]}[]};const message=body.messages.find(m=>m.To.some(to=>to.Address===target));if(message){const detail=await request.get(`http://127.0.0.1:54324/api/v1/message/${message.ID}`);const mail=await detail.json() as {HTML?:string;Text?:string};const links=(mail.HTML??mail.Text??"").match(/https?:\/\/[^\s"'<>]+/g)??[];const link=links.find(item=>item.includes("verify")||item.includes("token"))??links[0];if(link)return link.replace(/&amp;/g,"&")}await new Promise(resolve=>setTimeout(resolve,200))}throw new Error("Fictional team invitation did not arrive.")}
test("owner completes, publishes, unpublishes, and republishes onboarding",async({page,request,browser})=>{
 test.setTimeout(90_000);
 await request.delete("http://127.0.0.1:54324/api/v1/messages");await page.goto("/auth/sign-up");await page.getByLabel("Full name").fill("Fictional Linden Owner");await page.getByLabel("Email address").fill(email);await page.getByLabel("Password",{exact:true}).fill(password);await page.getByLabel("Confirm password").fill(password);await page.getByRole("button",{name:"Create account"}).click();await page.goto(await confirmationLink(request));
 await page.getByLabel("Choose image").setInputFiles({name:"ignored-logo-name.png",mimeType:"image/png",buffer:png});await page.getByRole("button",{name:"Upload image"}).click();await expect(page.getByText("Logo updated.")).toBeVisible();await page.reload();await expect(page.locator(".media-preview img")).toBeVisible();await page.getByLabel("Business display name").fill("Fictional Linden Studio");await page.getByLabel("Public address").fill(slug);await page.getByRole("button",{name:"Continue"}).click();await page.getByLabel("IANA timezone").fill("Europe/Bucharest");await page.getByLabel("Currency").selectOption("EUR");await page.getByLabel("Country code").fill("RO");await page.getByLabel("City").fill("Sample City");await page.getByRole("button",{name:"Continue"}).click();await page.getByRole("button",{name:"Continue"}).click();
 await expect(page.getByRole("heading",{name:"First staff profile"})).toBeVisible();await page.reload();await expect(page.getByRole("heading",{name:"First staff profile"})).toBeVisible();for(const width of [320,1440]){await page.setViewportSize({width,height:900});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true)}await page.emulateMedia({reducedMotion:"reduce"});await page.getByLabel("Public display name").fill("Fictional Linden Owner");await page.getByLabel("Job title (optional)").fill("Founder");await page.getByLabel("Short biography (optional)").fill("A fictional owner-operator profile used only for local testing.");await page.getByRole("button",{name:"Continue"}).click();await expect(page.getByRole("heading",{name:"First service"})).toBeVisible();await page.goto("/onboarding?step=staff-profile");await expect(page.getByRole("heading",{name:"First staff profile"})).toBeVisible();await page.getByLabel("Choose image").setInputFiles({name:"ignored-avatar-name.png",mimeType:"image/png",buffer:png});await page.getByRole("button",{name:"Upload image"}).click();await expect(page.getByText("Avatar updated.")).toBeVisible();await page.reload();await expect(page.locator(".media-preview img")).toBeVisible();await page.goto("/onboarding");
 await expect(page.getByRole("heading",{name:"First service"})).toBeVisible();await page.reload();await page.getByLabel("Service name").fill("Fictional Planning Session");await page.getByLabel("Description (optional)").fill("A fictional planning service.");await page.getByLabel("Duration (minutes)").fill("60");await page.getByLabel("Buffer after (minutes)").fill("15");await page.getByLabel("Price in EUR").fill("125.50");await page.getByRole("button",{name:"Continue"}).click();
 await expect(page.getByRole("heading",{name:"Weekly availability"})).toBeVisible();await page.reload();await expect(page.getByText("Europe/Bucharest",{exact:false})).toBeVisible();await page.getByLabel("Weekday").selectOption("1");await page.getByLabel("Start").fill("09:00");await page.getByLabel("End").fill("17:00");await page.getByRole("button",{name:"Add interval"}).click();await page.getByLabel("Weekday").nth(1).selectOption("3");await page.getByLabel("Start").nth(1).fill("10:00");await page.getByLabel("End").nth(1).fill("14:00");await page.getByRole("button",{name:"Continue"}).click();
 await expect(page.getByRole("heading",{name:"Review"})).toBeVisible();await page.reload();await expect(page.getByText("Fictional Planning Session")).toBeVisible();await expect(page.getByText("€125.50")).toBeVisible();await expect(page.getByText("2 weekly intervals")).toBeVisible();const preview=await page.getByRole("link",{name:"Preview your persisted draft"}).count();expect(preview).toBe(0);await page.getByRole("button",{name:"Confirm review"}).click();
 await expect(page.getByRole("heading",{name:"Publish"})).toBeVisible();await expect(page.getByRole("link",{name:"Preview your persisted draft"})).toBeVisible();const beforeContext=await browser.newContext();const anonymousBefore=await beforeContext.newPage();await anonymousBefore.goto(`/book/${slug}`);await expect(anonymousBefore.getByRole("heading",{name:"This booking page is not public."})).toBeVisible();await beforeContext.close();
 await page.getByRole("button",{name:"Publish business"}).click();await expect(page).toHaveURL(/\/dashboard\?published=1/);await expect(page.getByText("now live")).toBeVisible();await expect(page.getByRole("heading",{name:"Your business is published"})).toBeVisible();
 await page.getByRole("link",{name:"Manage services"}).click();await expect(page.getByRole("heading",{name:"Services"})).toBeVisible();await expect(page.getByText("Fictional Planning Session")).toBeVisible();await page.getByRole("link",{name:"Create service"}).click();await page.getByRole("button",{name:"Create service"}).click();await expect(page.getByText("Enter a service name.")).toBeVisible();await page.getByLabel("Public service name").fill("Fictional Focus Session");await page.getByLabel("Description (optional)").fill("A fictional Phase 4A service.");await page.getByLabel("Duration (minutes)").fill("45");await page.getByLabel("Buffer after (minutes)").fill("10");await page.getByLabel("Price (EUR)").fill("85.50");await page.getByRole("button",{name:"Create service"}).click();await expect(page.getByText("Service created successfully.")).toBeVisible();await page.getByLabel("Public service name").fill("Fictional Focus Session Updated");await page.getByRole("button",{name:"Save changes"}).click();await expect(page.getByText("Service details saved.")).toBeVisible();await page.getByRole("button",{name:"Assign"}).click();await expect(page.getByText("Staff assigned.")).toBeVisible();await page.getByRole("button",{name:"Archive service"}).click();await expect(page.getByText("Service archived.")).toBeVisible();await page.getByRole("button",{name:"Activate service"}).click();await expect(page.getByText("Service activated.")).toBeVisible();for(const width of [320,390,768,1024,1440]){await page.setViewportSize({width,height:900});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true)}const serviceAxe=await new AxeBuilder({page}).analyze();expect(serviceAxe.violations.filter(v=>["serious","critical"].includes(v.impact??""))).toEqual([]);await page.goto("/dashboard/services/23000000-0000-4000-8000-000000000201");await expect(page.getByRole("heading",{name:"Service not found"})).toBeVisible();await page.goto("/dashboard");
 const anonymousContext=await browser.newContext();const anonymous=await anonymousContext.newPage();await anonymous.goto(`/book/${slug}`);await expect(anonymous.getByRole("heading",{name:"Fictional Linden Studio"})).toBeVisible();await expect(anonymous.getByAltText("Fictional Linden Studio logo")).toBeVisible();await expect(anonymous.getByAltText("Fictional Linden Owner profile photo")).toBeVisible();await expect(anonymous.getByRole("link",{name:/Choose Fictional Planning Session/})).toBeVisible();for(const width of [320,390,768,1024,1440]){await anonymous.setViewportSize({width,height:900});await anonymous.reload();expect(await anonymous.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true)}const axe=await new AxeBuilder({page:anonymous}).analyze();expect(axe.violations.filter(v=>["serious","critical"].includes(v.impact??""))).toEqual([]);await anonymousContext.close();
 const inviteeEmail="phase4b-invitee@serviceflow.test";await request.delete("http://127.0.0.1:54324/api/v1/messages");await page.goto("/dashboard/team");await expect(page.getByRole("heading",{name:"Team"})).toBeVisible();await page.getByRole("link",{name:"Invite team member"}).click();await page.getByLabel("Work email").fill("invalid");await page.getByRole("button",{name:"Send invitation"}).click();await expect(page.getByText("Enter a valid email address.")).toBeVisible();await page.getByLabel("Work email").fill(inviteeEmail.toUpperCase());await page.getByRole("button",{name:"Send invitation"}).click();await expect(page.getByText("Invitation created.")).toBeVisible();await expect(page.getByText(inviteeEmail)).toBeVisible();await page.getByRole("button",{name:"Resend"}).click();await expect(page.getByText("Invitation sent again.")).toBeVisible();for(const width of [320,390,768,1024,1440]){await page.setViewportSize({width,height:900});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true)}const teamAxe=await new AxeBuilder({page}).analyze();expect(teamAxe.violations.filter(v=>["serious","critical"].includes(v.impact??""))).toEqual([]);const inviteeContext=await browser.newContext();const invitee=await inviteeContext.newPage();await invitee.goto(await invitationLink(request,inviteeEmail));await expect(invitee.getByRole("heading",{name:"Join Fictional Linden Studio"})).toBeVisible();await invitee.getByRole("button",{name:"Accept and join team"}).click();await expect(invitee.getByText("Welcome to the team.")).toBeVisible();await inviteeContext.close();await page.reload();await page.getByRole("link",{name:"Phase4b-invitee"}).click();await page.getByLabel("Display name").fill("Fictional Invited Specialist");await page.getByLabel("Professional title").fill("Guest specialist");await page.getByLabel("Short biography").fill("A fictional invited team profile.");await page.getByRole("button",{name:"Save profile"}).click();await expect(page.getByText("Team profile saved.")).toBeVisible();await page.getByRole("button",{name:"Deactivate member"}).click();await page.getByRole("button",{name:"Confirm deactivation"}).click();await expect(page.getByText("removed from active service assignments")).toBeVisible();await page.goto("/dashboard/team");await expect(page.getByText("inactive",{exact:true}).first()).toBeVisible();
 const oldLogo=await page.goto("/dashboard").then(()=>page.getByAltText("Fictional Linden Studio logo").getAttribute("src"));await page.getByLabel("Choose image").first().setInputFiles({name:"replacement.webp",mimeType:"image/webp",buffer:png});await page.getByRole("button",{name:"Replace image"}).first().click();await expect(page.getByText("Logo updated.")).toBeVisible();await expect(page.getByAltText("Fictional Linden Studio logo")).not.toHaveAttribute("src",oldLogo??"");await page.getByRole("button",{name:"Remove image"}).last().click();await expect(page.getByText("Avatar removed.")).toBeVisible();await expect(page.getByTestId("avatar-fallback")).toBeVisible();await expect(page.getByRole("heading",{name:"Your business is published"})).toBeVisible();await page.getByLabel("Choose image").first().setInputFiles({name:"unsafe.svg",mimeType:"image/svg+xml",buffer:Buffer.from("<svg></svg>")});await expect(page.getByText("Choose a JPEG, PNG, or WebP image.")).toBeVisible();await page.getByLabel("Choose image").first().setInputFiles({name:"large.png",mimeType:"image/png",buffer:Buffer.alloc(2*1024*1024+1)});await expect(page.getByText("The image must be 2 MB or smaller.")).toBeVisible();const mediaContext=await browser.newContext();const mediaPublic=await mediaContext.newPage();await mediaPublic.goto(`/book/${slug}`);await expect(mediaPublic.getByAltText("Fictional Linden Studio logo")).toBeVisible();await expect(mediaPublic.getByTestId("media-fallback")).toBeVisible();await mediaContext.close();
 await page.getByRole("button",{name:"Unpublish"}).click();await expect(page.getByText("I understand the public profile")).toBeVisible();await page.getByLabel("I understand the public profile will become unavailable immediately.").check();await page.getByRole("button",{name:"Confirm unpublish"}).click();await expect(page.getByText("Business unpublished")).toBeVisible();const unavailableContext=await browser.newContext();const unavailable=await unavailableContext.newPage();await unavailable.goto(`/book/${slug}`);await expect(unavailable.getByRole("heading",{name:"This booking page is not public."})).toBeVisible();await unavailableContext.close();await page.reload();await page.getByRole("button",{name:"Republish business"}).click();await expect(page.getByText("Business republished.")).toBeVisible();
 await page.keyboard.press("Tab");await expect(page.locator(":focus-visible")).toBeVisible();
});
