import Link from "next/link";
import {notFound} from "next/navigation";
import {getBookingConfirmation} from "@/features/booking/booking-server";
import {publicSlugSchema} from "@/features/booking/schemas";
import {formatMinorPrice} from "@/features/onboarding/schemas";
type Props={params:Promise<{slug:string;reference:string}>;searchParams:Promise<{token?:string|string[]}>};
export const dynamic="force-dynamic";
export default async function Page({params,searchParams}:Props){const route=await params,slug=publicSlugSchema.safeParse(route.slug);if(!slug.success||!/^SF-[A-Z0-9-]{16,64}$/.test(route.reference))notFound();const query=await searchParams,token=typeof query.token==="string"?query.token:undefined;const booking=await getBookingConfirmation(route.reference,token);if(!booking||booking.slug!==slug.data)notFound();const localTime=new Intl.DateTimeFormat("en",{timeZone:booking.timezone,weekday:"long",year:"numeric",month:"long",day:"numeric",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).format(new Date(booking.startsAt));return <main id="main-content" className="booking-page"><header className="booking-header"><Link className="brand-link" href="/">ServiceFlow</Link></header><section className="booking-shell confirmation-success" aria-labelledby="confirmation-heading"><div className="confirmation-check" aria-hidden="true">✓</div><p className="eyebrow">Appointment confirmed</p><h1 id="confirmation-heading">You’re booked with {booking.business}</h1><p role="status">Your no-payment appointment was created successfully. Email delivery is not enabled in this phase, so keep this private link.</p><section className="booking-review"><h2>Confirmation details</h2><dl><div><dt>Reference</dt><dd>{booking.reference}</dd></div><div><dt>Service</dt><dd>{booking.service}</dd></div><div><dt>Team member</dt><dd>{booking.staff}</dd></div><div><dt>Local time</dt><dd>{localTime}</dd></div><div><dt>Duration</dt><dd>{booking.durationMinutes} minutes</dd></div><div><dt>Price</dt><dd>{formatMinorPrice(booking.priceMinor,booking.currency)} · No online payment</dd></div><div><dt>Timezone</dt><dd>{booking.timezone}</dd></div><div><dt>Contact</dt><dd>{booking.clientName}<br/>{booking.clientEmail}{booking.clientPhone?<><br/>{booking.clientPhone}</>:null}</dd></div></dl></section><p className="booking-fine-print">This page can be reloaded with its private access token. Do not share the link publicly.</p><Link className="button button-ink" href={`/book/${slug.data}`}>Back to {booking.business}</Link></section></main>}
