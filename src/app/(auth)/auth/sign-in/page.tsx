import { AuthForm } from "@/components/auth/auth-form"; import { signInAction } from "@/features/auth/actions"; import { safeReturnPath } from "@/features/auth/return-path";
export const metadata = { title: "Sign in" }; export default async function Page({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const params = await searchParams; const notice = params.authError === "missing" ? "The authentication link is incomplete. Request a new link." : params.authError === "expired" ? "The authentication link is invalid or expired. Request a new link." : undefined; return <AuthForm kind="sign-in" action={signInAction} next={typeof params.next === "string" ? safeReturnPath(params.next) : undefined} notice={notice} />; }
