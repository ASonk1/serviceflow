import Link from "next/link";import {ManagementPagination} from "@/components/data-display/management-pagination";import {getAvailabilityList} from "@/features/availability/workspace";import {availabilityState,listUrl,parseAvailabilityListQuery,type RawSearchParams} from "@/features/lists/query";
export const metadata={title:"Availability"};
export default async function Page({searchParams}:{searchParams:Promise<RawSearchParams>}){const query=parseAvailabilityListQuery(await searchParams);const{organization,result,viewerRole}=await getAvailabilityList(query);const state=availabilityState({...query,page:result.page});return <main id="main-content" className="dashboard-foundation service-management availability-management"><header className="dashboard-header"><div><Link href="/dashboard" className="back-link">← Dashboard</Link><p className="eyebrow">Schedules</p><h1>Availability</h1><p>All times are managed in <strong>{organization.timezone}</strong>.</p></div></header><section className="service-editor-card"><h2>{viewerRole==="owner"?"Choose a team member":"Your schedule"}</h2><p>{viewerRole==="owner"?"Only active team members can have their schedules managed.":"You can manage only your own recurring hours and blocked time."}</p><form className="management-filters" method="get" role="search"><div className="form-field"><label htmlFor="schedule-search">Search schedule members</label><input id="schedule-search" name="q" defaultValue={query.q} placeholder="Name or professional title"/></div><div className="form-field"><label htmlFor="schedule-sort">Sort by</label><select id="schedule-sort" name="sort" defaultValue={query.sort}><option value="name">Name</option><option value="role">Role</option></select></div><div className="form-field"><label htmlFor="schedule-dir">Direction</label><select id="schedule-dir" name="dir" defaultValue={query.dir}><option value="asc">Ascending</option><option value="desc">Descending</option></select></div><div className="form-field"><label htmlFor="schedule-size">Per page</label><select id="schedule-size" name="size" defaultValue={query.size}><option value="5">5</option><option value="10">10</option><option value="20">20</option></select></div><div className="management-filter-actions"><button className="button button-primary">Apply</button>{query.q?<Link className="button onboarding-secondary" href="/dashboard/availability">Reset search</Link>:null}</div></form><p className="sort-summary">Sorted by {query.sort}, {query.dir==="asc"?"ascending":"descending"}.</p>{result.items.length?<><div className="team-grid">{result.items.map(member=><article className="team-card" key={member.id}><div className="team-card-heading"><span className="generated-avatar" aria-hidden="true">{member.display_name.slice(0,2).toUpperCase()}</span><div><h3>{member.display_name}</h3><p>{member.job_title??"Team member"} · {member.role}</p></div></div><Link className="button button-primary" href={`/dashboard/availability/${member.id}`}>Manage schedule</Link></article>)}</div><ManagementPagination page={result.page} pageSize={result.pageSize} total={result.total} previousHref={listUrl("/dashboard/availability",{...state,page:result.page-1})} nextHref={listUrl("/dashboard/availability",{...state,page:result.page+1})}/></>:result.organizationTotal===0?<div className="service-empty"><h3>No active schedule profile</h3><p>Ask an owner to activate your team membership and profile.</p></div>:<div className="service-empty"><h3>No matching team members</h3><p>Try a broader search.</p><Link className="button onboarding-secondary" href="/dashboard/availability">Reset search</Link></div>}</section></main>}
