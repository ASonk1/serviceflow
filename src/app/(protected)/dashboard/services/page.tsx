import Link from "next/link";
import {ManagementPagination} from "@/components/data-display/management-pagination";
import {listUrl,parseServiceListQuery,serviceState,type RawSearchParams} from "@/features/lists/query";
import {formatServicePrice} from "@/features/services/schemas";
import {getServiceList} from "@/features/services/workspace";
export const metadata={title:"Services"};
export default async function Page({searchParams}:{searchParams:Promise<RawSearchParams>}){const query=parseServiceListQuery(await searchParams);const{organization,result}=await getServiceList(query);const active=Boolean(query.q||query.status!=="all");const state=serviceState({...query,page:result.page});return <main id="main-content" className="dashboard-foundation service-management"><header className="dashboard-header"><div><Link href="/dashboard" className="back-link">← Owner dashboard</Link><p className="eyebrow">Service catalog</p><h1>Services</h1><p>{organization.name} · {organization.currency} · {organization.timezone}</p></div><Link className="button button-primary" href="/dashboard/services/new">Create service</Link></header><form className="management-filters" method="get" role="search"><div className="form-field"><label htmlFor="service-search">Search services</label><input id="service-search" name="q" defaultValue={query.q} placeholder="Name or description"/></div><div className="form-field"><label htmlFor="service-status">Status</label><select id="service-status" name="status" defaultValue={query.status}><option value="all">All statuses</option><option value="active">Active</option><option value="draft">Draft</option><option value="archived">Archived</option></select></div><div className="form-field"><label htmlFor="service-sort">Sort by</label><select id="service-sort" name="sort" defaultValue={query.sort}><option value="name">Name</option><option value="status">Status</option><option value="price">Price</option><option value="updated">Last updated</option></select></div><div className="form-field"><label htmlFor="service-dir">Direction</label><select id="service-dir" name="dir" defaultValue={query.dir}><option value="asc">Ascending</option><option value="desc">Descending</option></select></div><div className="form-field"><label htmlFor="service-size">Per page</label><select id="service-size" name="size" defaultValue={query.size}><option value="5">5</option><option value="10">10</option><option value="20">20</option></select></div><div className="management-filter-actions"><button className="button button-primary" type="submit">Apply</button>{active?<Link className="button onboarding-secondary" href="/dashboard/services">Reset filters</Link>:null}</div></form><p className="sort-summary">Sorted by {query.sort}, {query.dir==="asc"?"ascending":"descending"}.</p>{result.items.length?<><div className="service-list">{result.items.map(service=><article className="service-list-card" key={service.id}><div><span className={`service-status service-status-${service.status}`}>{service.status}</span><h2><Link href={`/dashboard/services/${service.id}`}>{service.name}</Link></h2><p>{service.description||"No public description."}</p></div><dl><div><dt>Price</dt><dd>{formatServicePrice(Number(service.price_minor),organization.currency)}</dd></div><div><dt>Duration</dt><dd>{service.duration_minutes} min + {service.buffer_after_minutes} min buffer</dd></div><div><dt>Assigned staff</dt><dd>{service.active_staff_count}</dd></div></dl><Link className="button onboarding-secondary" href={`/dashboard/services/${service.id}`}>View and edit</Link></article>)}</div><ManagementPagination page={result.page} pageSize={result.pageSize} total={result.total} previousHref={listUrl("/dashboard/services",{...state,page:result.page-1})} nextHref={listUrl("/dashboard/services",{...state,page:result.page+1})}/></>:result.organizationTotal===0?<section className="service-empty"><h2>No services yet</h2><p>Create a fictional service to begin building the catalog. Services are never hard-deleted.</p><Link className="button button-primary" href="/dashboard/services/new">Create your first service</Link></section>:<section className="service-empty"><h2>No matching services</h2><p>Try a broader search or remove a status filter.</p><Link className="button onboarding-secondary" href="/dashboard/services">Reset filters</Link></section>}</main>}
