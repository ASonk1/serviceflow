import Link from "next/link";
import type {PublicBusiness} from "@/features/onboarding/public-business";
import {formatMinorPrice} from "@/features/onboarding/schemas";
import {getPublicEnvironment} from "@/lib/env/public";
import {publicMediaUrl} from "@/features/media/validation";
import {PublicMediaImage} from "./public-media-image";
function paymentCopy(service:PublicBusiness["services"][number],currency:string){if(service.paymentMode==="deposit"&&service.depositMinor)return `${formatMinorPrice(service.depositMinor,currency)} deposit`;if(service.paymentMode==="full")return "Full payment required later";return "No online payment required"}
export function BusinessProfile({business,preview=false}:{business:PublicBusiness;preview?:boolean}){const base=getPublicEnvironment().NEXT_PUBLIC_SUPABASE_URL;return <main id="main-content" className="business-profile-page"><header className="profile-header"><Link href="/" className="brand-link">ServiceFlow</Link>{preview?<span className="draft-badge">Private draft preview</span>:<span className="public-badge">Open for booking</span>}</header><section className="profile-hero"><div className="profile-identity"><PublicMediaImage className="organization-logo" src={publicMediaUrl(base,business.logoPath)} alt={`${business.name} logo`} name={business.name}/><div><p className="eyebrow">Book with ServiceFlow</p><h1>{business.name}</h1><p>{[business.city,business.region,business.countryCode].filter(Boolean).join(", ")} · Times shown in {business.timezone}</p>{business.description?<p>{business.description}</p>:null}</div></div><div className="profile-cta"><strong>Choose a service</strong><p>You’ll choose a team member, local date, and available time next. No appointment is reserved during this step.</p></div></section><div className="profile-sections"><section aria-labelledby="services-title"><h2 id="services-title">Services</h2>{business.services.length?<div className="public-card-grid">{business.services.map(service=><article className="public-card" key={service.id}><h3>{service.name}</h3>{service.description?<p>{service.description}</p>:null}<dl><div><dt>Appointment</dt><dd>{service.durationMinutes} minutes</dd></div>{service.bufferMinutes?<div><dt>Cleanup time</dt><dd>{service.bufferMinutes} minutes</dd></div>:null}<div><dt>Price</dt><dd>{formatMinorPrice(service.priceMinor,business.currency)}</dd></div><div><dt>Payment</dt><dd>{paymentCopy(service,business.currency)}</dd></div></dl>{preview?<button className="button button-primary" disabled>Preview only</button>:<Link className="button button-primary" href={`/book/${business.slug}/availability?service=${service.id}`}>Choose {service.name}</Link>}</article>)}</div>:<p className="public-empty">No bookable services are currently available.</p>}</section><section aria-labelledby="team-title"><h2 id="team-title">Team</h2>{business.staff.map(person=><article className="staff-public-card" key={person.displayName}><PublicMediaImage src={publicMediaUrl(base,person.avatarPath)} alt={`${person.displayName} profile photo`} name={person.displayName}/><div><h3>{person.displayName}</h3><strong>{person.jobTitle??"Team member"}</strong>{person.bio?<p>{person.bio}</p>:null}</div></article>)}</section></div></main>}
