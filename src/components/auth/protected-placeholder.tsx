import Link from "next/link"; import { signOutAction } from "@/features/auth/actions";
export function ProtectedPlaceholder({ eyebrow, title }: { eyebrow: string; title: string }) { return <main id="main-content" className="protected-page"><section className="protected-card"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>Your secure destination is ready. Operational tools and real dashboard functionality arrive in later ServiceFlow phases.</p><div className="protected-actions"><Link className="button button-secondary" href="/">Marketing site</Link><form action={signOutAction}><button className="button button-ink" type="submit">Sign out</button></form></div></section></main>; }
